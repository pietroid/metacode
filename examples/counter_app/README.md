# Counter App

The smallest Metacode example: one store holding an int, two buttons, and
seven scenarios. This is the one to read first.

```bash
cd examples/counter_app
go run ../../engine/cmd/metacode run
flutter test
```

The specs are in `metacode/`. Everything under `lib/` and `test/` is generated.
