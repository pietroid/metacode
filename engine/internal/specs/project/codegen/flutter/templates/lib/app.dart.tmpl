{{if .UsesRouter}}// GENERATED BY METACODE - DO NOT EDIT BY HAND
// Source spec: navigation.yaml

import 'package:flutter/material.dart';
import 'navigation/router.dart';

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  // Static, so the widget stays const-constructible and the router is built
  // once. A GoRouter holds the navigation stack, so building one per rebuild
  // would reset it.
  static final _router = buildRouter();

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      title: {{.Title}},
      routerConfig: _router,
    );
  }
}
{{else}}// GENERATED BY METACODE - DO NOT EDIT BY HAND
// Source spec: ui.yaml > {{.PageName}}

import 'package:flutter/material.dart';
import 'pages/{{.FileName}}';

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: {{.Title}},
      home: const {{.ClassName}}{{.ArgList}},
    );
  }
}
{{end -}}
